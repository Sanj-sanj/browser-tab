Background useEffect watches context's state: background for changes. Makes 2 network calls to internal server and returns image blob, and alt text. Application window now houses the container for Settings, made function to return jsx based on import names filtered by using context background string as property name on a for in loop. Context menu now accepts  context as a destructurable prop, to open Settings and further capabilities. useMenu settings button altered for similar purpose. Button accepts an onclick now too for opening stuff capabiliies...
