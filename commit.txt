NavAppWindow has has a highlight uptop. FileSystemApp had two functions pulled out and placed into /js. UserContext hard-coded 'apps' now have an id on them, these are used when looping over icons as a unique key, a key is created on dispatch by uuid, hardcoded are just 3digit numbers.
