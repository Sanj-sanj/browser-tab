Screen got brightness div added, background now just the background. Application had logic seperated into smaller components. Context menu now renders menu based on Context data. UserContext and dispatch updates with props and dispatch's for this reason. Nav now closes on ContextMenu creation, and clickign nav while contextMenu open closes it. tailwindconfig has new animations for opening and closing appWindow.

