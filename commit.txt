Deskotp: added useMobileTouchHandler hook to desktop,
AppWindow: removed elref onclick handler, now pass clearDesktopContext as prop, the onclick was interfering with the contextMenu inside of the filesystem. (click open or rename, resets state onclick) Filesystem now has contextmenu for icons enabled. newFolderApp now working for mobile.
Icon: e.stopPropagation added.
style.css: added * [vendor-pre]user-select 'none' and 'text' to inputs. 
