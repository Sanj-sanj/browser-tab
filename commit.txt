Dispatch mkdir updated to accept a payload.whichDir to set the payload destination, changes within dispatch and userContext, opeNewFolder > openNewFolderApp. Icon added whitespace line. ContextMenu and fileSystemApp have mkdir function added and updated. NavAppWindow has thick prop, now a lil thicker. SettingsApp had the img imports added into it from AppWindow. Appbutton style update no pr. Filesystem app now has folder-img imports, set programmatically in the for-in loop function. Added a button to make folders, the navigational buttons dont do anything yet.
