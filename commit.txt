Icon: added ternary check for onClick, stops onCLick from applying on files	
Lock: added padding to better vertically center contents.
Desktop: onTouchStart checks to see if user is touching desktop or not then delegates event.
AppButton: received styles for mobile view.
App's: added more selectors for Draggable cancel prop, this is for mobile use.

